@page "/admin/issues"
@attribute [Authorize(Roles = "Admin")]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/admin?tab=issues", replace: true);
}

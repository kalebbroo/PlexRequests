@page "/admin/requests"
@attribute [Authorize(Roles = "Admin")]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/admin?tab=approvals", replace: true);
}

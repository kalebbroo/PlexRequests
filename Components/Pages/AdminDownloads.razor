@page "/admin/downloads"
@attribute [Authorize(Roles = "Admin")]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/admin?tab=downloads", replace: true);
}
